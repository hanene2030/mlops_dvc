from setuptools import setup, find_packages

setup(
    name="src",
    version="0.0.1",
    description="its a pricing package",
    author="hjem",
    packages=find_packages(),
    license="To define"
)
